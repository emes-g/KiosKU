import konkuk.swarchitecture.team6.PaymentManagerProxy;
import org.junit.Assert;
import org.junit.Test;

public class PaymentCheckTest {

    private PaymentManagerProxy paymentManagerProxy;

    @Test
    public void 계산금액_일치체크() {
        //given

        //when

        //then
        Assert.assertEquals("값이 같아야 합니다", 100, 100);
    }
}
